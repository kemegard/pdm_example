# Enable DMIC (PDM) driver
CONFIG_AUDIO=y
CONFIG_AUDIO_DMIC=y

# Logging
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3

# Main thread stack - analysis loop needs a bit of headroom
CONFIG_MAIN_STACK_SIZE=4096
